diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/adjustment/GetAllAdjustmentVouchersUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/adjustment/GetAllAdjustmentVouchersUseCase.java
index 2e1452b..36a3e6c 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/adjustment/GetAllAdjustmentVouchersUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/adjustment/GetAllAdjustmentVouchersUseCase.java
@@ -3,6 +3,7 @@ package org.lvtn.mws.application.usecases.adjustment;
 import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.AdjustmentVoucher;
 import org.lvtn.mws.domain.service.AdjustmentDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseScoped;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -15,7 +16,9 @@ public class GetAllAdjustmentVouchersUseCase {
 
     private final AdjustmentDomainService adjustmentDomainService;
 
+    /** A2: lọc phiếu điều chỉnh theo kho user được phép; ADMIN thấy tất cả. */
+    @WarehouseScoped
     public List<AdjustmentVoucher> execute() {
-        return adjustmentDomainService.findAll();
+        return adjustmentDomainService.findAllScoped();
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/purchaseorder/CreatePurchaseOrderUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/purchaseorder/CreatePurchaseOrderUseCase.java
index 1340de1..82045bc 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/purchaseorder/CreatePurchaseOrderUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/purchaseorder/CreatePurchaseOrderUseCase.java
@@ -4,6 +4,7 @@ import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.PurchaseOrder;
 import org.lvtn.mws.domain.model.PurchaseOrderLineCommand;
 import org.lvtn.mws.domain.service.PurchaseOrderDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseAccessGuard;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -14,10 +15,13 @@ import java.util.List;
 @RequiredArgsConstructor
 public class CreatePurchaseOrderUseCase {
     private final PurchaseOrderDomainService domainService;
+    private final WarehouseAccessGuard warehouseAccessGuard;
 
     @Transactional
     public PurchaseOrder execute(String supplierId, String warehouseId, LocalDate expectedDate,
                                  String createdBy, List<PurchaseOrderLineCommand> lines) {
+        // A2: chặn tạo phiếu mua nhập về kho ngoài phạm vi được giao (403).
+        warehouseAccessGuard.check(warehouseId);
         return domainService.create(supplierId, warehouseId, expectedDate, createdBy, lines);
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/CreateSalesOrderUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/CreateSalesOrderUseCase.java
index 0f20001..b5a1954 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/CreateSalesOrderUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/CreateSalesOrderUseCase.java
@@ -4,6 +4,7 @@ import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.SalesOrder;
 import org.lvtn.mws.domain.model.SalesOrderLineCommand;
 import org.lvtn.mws.domain.service.SalesOrderDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseAccessGuard;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -17,6 +18,7 @@ import java.util.List;
 public class CreateSalesOrderUseCase {
 
     private final SalesOrderDomainService salesOrderDomainService;
+    private final WarehouseAccessGuard warehouseAccessGuard;
 
     public SalesOrder execute(String warehouseId,
                               String customerId,
@@ -25,6 +27,8 @@ public class CreateSalesOrderUseCase {
                               LocalDate requiredDate,
                               String createdBy,
                               List<SalesOrderLineCommand> lines) {
+        // A2: chặn tạo đơn xuất cho kho ngoài phạm vi được giao (403).
+        warehouseAccessGuard.check(warehouseId);
         return salesOrderDomainService.create(
                 warehouseId, customerId, discountAmount, priority, requiredDate, createdBy, lines);
     }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/GetAllSalesOrdersUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/GetAllSalesOrdersUseCase.java
index e278290..7f9dfaa 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/GetAllSalesOrdersUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/salesorder/GetAllSalesOrdersUseCase.java
@@ -3,6 +3,7 @@ package org.lvtn.mws.application.usecases.salesorder;
 import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.SalesOrder;
 import org.lvtn.mws.domain.service.SalesOrderDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseScoped;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -15,7 +16,9 @@ public class GetAllSalesOrdersUseCase {
 
     private final SalesOrderDomainService salesOrderDomainService;
 
+    /** A2: lọc đơn xuất theo kho user được phép; ADMIN thấy tất cả. */
+    @WarehouseScoped
     public List<SalesOrder> execute() {
-        return salesOrderDomainService.findAll();
+        return salesOrderDomainService.findAllScoped();
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/shipment/ShipShipmentUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/shipment/ShipShipmentUseCase.java
index 047c435..347cb36 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/shipment/ShipShipmentUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/shipment/ShipShipmentUseCase.java
@@ -4,6 +4,7 @@ import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.application.event.ShipmentShippedEvent;
 import org.lvtn.mws.domain.model.Shipment;
 import org.lvtn.mws.domain.service.ShipmentDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseAccessGuard;
 import org.springframework.context.ApplicationEventPublisher;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
@@ -20,8 +21,11 @@ public class ShipShipmentUseCase {
 
     private final ShipmentDomainService shipmentDomainService;
     private final ApplicationEventPublisher eventPublisher;
+    private final WarehouseAccessGuard warehouseAccessGuard;
 
     public Shipment execute(String shipmentId, String actorUserId, String warehouseId) {
+        // A2: chặn xuất hàng khỏi kho ngoài phạm vi được giao (403).
+        warehouseAccessGuard.check(warehouseId);
         Shipment shipment = shipmentDomainService.ship(shipmentId);
         if (shipment.isSalesShipment()) {
             eventPublisher.publishEvent(new ShipmentShippedEvent(
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/GetAllStocktakesUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/GetAllStocktakesUseCase.java
index 9e4e76f..a16e571 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/GetAllStocktakesUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/GetAllStocktakesUseCase.java
@@ -3,6 +3,7 @@ package org.lvtn.mws.application.usecases.stocktake;
 import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.StocktakeSession;
 import org.lvtn.mws.domain.service.StocktakeDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseScoped;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -15,7 +16,9 @@ public class GetAllStocktakesUseCase {
 
     private final StocktakeDomainService stocktakeDomainService;
 
+    /** A2: lọc phiên kiểm kê theo kho user được phép; ADMIN thấy tất cả. */
+    @WarehouseScoped
     public List<StocktakeSession> execute() {
-        return stocktakeDomainService.findAll();
+        return stocktakeDomainService.findAllScoped();
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/StartStocktakeUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/StartStocktakeUseCase.java
index dbe434b..3ddb05c 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/StartStocktakeUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/stocktake/StartStocktakeUseCase.java
@@ -3,6 +3,7 @@ package org.lvtn.mws.application.usecases.stocktake;
 import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.StocktakeSession;
 import org.lvtn.mws.domain.service.StocktakeDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseAccessGuard;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -12,9 +13,12 @@ import org.springframework.transaction.annotation.Transactional;
 public class StartStocktakeUseCase {
 
     private final StocktakeDomainService stocktakeDomainService;
+    private final WarehouseAccessGuard warehouseAccessGuard;
 
     @Transactional
     public StocktakeSession execute(String warehouseId, String createdBy) {
+        // A2: chặn khởi tạo kiểm kê trên kho ngoài phạm vi được giao (403).
+        warehouseAccessGuard.check(warehouseId);
         return stocktakeDomainService.startStocktake(warehouseId, createdBy);
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/application/usecases/transfer/GetAllTransferOrdersUseCase.java b/backend/src/main/java/org/lvtn/mws/application/usecases/transfer/GetAllTransferOrdersUseCase.java
index 2805bf4..4ccb5f1 100644
--- a/backend/src/main/java/org/lvtn/mws/application/usecases/transfer/GetAllTransferOrdersUseCase.java
+++ b/backend/src/main/java/org/lvtn/mws/application/usecases/transfer/GetAllTransferOrdersUseCase.java
@@ -3,6 +3,7 @@ package org.lvtn.mws.application.usecases.transfer;
 import lombok.RequiredArgsConstructor;
 import org.lvtn.mws.domain.model.TransferOrder;
 import org.lvtn.mws.domain.service.TransferOrderDomainService;
+import org.lvtn.mws.infrastructure.security.scope.WarehouseScoped;
 import org.springframework.stereotype.Service;
 import org.springframework.transaction.annotation.Transactional;
 
@@ -15,7 +16,9 @@ public class GetAllTransferOrdersUseCase {
 
     private final TransferOrderDomainService transferOrderDomainService;
 
+    /** A2: lọc phiếu theo kho user được phép (nguồn hoặc đích); ADMIN thấy tất cả. */
+    @WarehouseScoped
     public List<TransferOrder> execute() {
-        return transferOrderDomainService.findAll();
+        return transferOrderDomainService.findAllScoped();
     }
 }
diff --git a/backend/src/main/java/org/lvtn/mws/domain/repository/IAdjustmentVoucherRepository.java b/backend/src/main/java/org/lvtn/mws/domain/repository/IAdjustmentVoucherRepository.java
index 109dd0c..95e9578 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/repository/IAdjustmentVoucherRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/repository/IAdjustmentVoucherRepository.java
@@ -8,6 +8,8 @@ public interface IAdjustmentVoucherRepository {
     AdjustmentVoucher save(AdjustmentVoucher voucher);
     Optional<AdjustmentVoucher> findById(String id);
     List<AdjustmentVoucher> findAll();
+    /** [A2] Chỉ phiếu điều chỉnh thuộc kho user được phép (đọc từ WarehouseScopeContext). */
+    List<AdjustmentVoucher> findAllScoped();
     List<AdjustmentVoucher> findBySessionId(String sessionId);
     boolean existsByVoucherNumber(String voucherNumber);
 }
diff --git a/backend/src/main/java/org/lvtn/mws/domain/repository/ISalesOrderRepository.java b/backend/src/main/java/org/lvtn/mws/domain/repository/ISalesOrderRepository.java
index 561c61c..46b31ee 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/repository/ISalesOrderRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/repository/ISalesOrderRepository.java
@@ -11,6 +11,8 @@ public interface ISalesOrderRepository {
     SalesOrder save(SalesOrder salesOrder);
     Optional<SalesOrder> findById(String id);
     List<SalesOrder> findAll();
+    /** [A2] Chỉ đơn thuộc kho user được phép (đọc từ WarehouseScopeContext). */
+    List<SalesOrder> findAllScoped();
     List<SalesOrder> findByStatus(Status status);
     boolean existsBySoNumber(String soNumber);
 }
diff --git a/backend/src/main/java/org/lvtn/mws/domain/repository/IStocktakeSessionRepository.java b/backend/src/main/java/org/lvtn/mws/domain/repository/IStocktakeSessionRepository.java
index bc92ac6..351cedf 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/repository/IStocktakeSessionRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/repository/IStocktakeSessionRepository.java
@@ -8,6 +8,8 @@ public interface IStocktakeSessionRepository {
     StocktakeSession save(StocktakeSession session);
     Optional<StocktakeSession> findById(String id);
     List<StocktakeSession> findAll();
+    /** [A2] Chỉ phiên kiểm kê thuộc kho user được phép (đọc từ WarehouseScopeContext). */
+    List<StocktakeSession> findAllScoped();
     List<StocktakeSession> findByWarehouseId(String warehouseId);
 
     /** Phiên đang FREEZED của một kho (nếu có) — dùng cho cơ chế đóng băng kho. */
diff --git a/backend/src/main/java/org/lvtn/mws/domain/repository/ITransferOrderRepository.java b/backend/src/main/java/org/lvtn/mws/domain/repository/ITransferOrderRepository.java
index 37aed74..5d53ae8 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/repository/ITransferOrderRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/repository/ITransferOrderRepository.java
@@ -13,6 +13,8 @@ public interface ITransferOrderRepository {
     Optional<TransferOrder> findById(String id);
 
     List<TransferOrder> findAll();
+    /** [A2] Chỉ phiếu mà user có quyền ở kho nguồn HOẶC kho đích (đọc từ WarehouseScopeContext). */
+    List<TransferOrder> findAllScoped();
 
     List<TransferOrder> findByStatus(TransferOrder.Status status);
 
diff --git a/backend/src/main/java/org/lvtn/mws/domain/service/AdjustmentDomainService.java b/backend/src/main/java/org/lvtn/mws/domain/service/AdjustmentDomainService.java
index 6bd7f12..2391696 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/service/AdjustmentDomainService.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/service/AdjustmentDomainService.java
@@ -67,6 +67,9 @@ public class AdjustmentDomainService {
 
     public List<AdjustmentVoucher> findAll() { return voucherRepository.findAll(); }
 
+    /** [A2] Danh sách phiếu điều chỉnh đã lọc theo phạm vi kho của user. */
+    public List<AdjustmentVoucher> findAllScoped() { return voucherRepository.findAllScoped(); }
+
     // ── 1. Sinh phiếu điều chỉnh (DRAFT) từ phiên kiểm kê ────────────────────
 
     /**
diff --git a/backend/src/main/java/org/lvtn/mws/domain/service/SalesOrderDomainService.java b/backend/src/main/java/org/lvtn/mws/domain/service/SalesOrderDomainService.java
index c96cbdf..8f39c05 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/service/SalesOrderDomainService.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/service/SalesOrderDomainService.java
@@ -80,6 +80,11 @@ public class SalesOrderDomainService {
         return soRepository.findAll();
     }
 
+    /** [A2] Danh sách đơn xuất đã lọc theo phạm vi kho của user. */
+    public List<SalesOrder> findAllScoped() {
+        return soRepository.findAllScoped();
+    }
+
     /**
      * Phân bổ hàng ảo: duyệt từng dòng, gọi reserve trên tồn kho tổng.
      * Nếu bất kỳ dòng nào không đủ -> ném InsufficientStockException để UseCase rollback toàn bộ.
diff --git a/backend/src/main/java/org/lvtn/mws/domain/service/StocktakeDomainService.java b/backend/src/main/java/org/lvtn/mws/domain/service/StocktakeDomainService.java
index d4f331a..417f8cc 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/service/StocktakeDomainService.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/service/StocktakeDomainService.java
@@ -41,6 +41,9 @@ public class StocktakeDomainService {
 
     public List<StocktakeSession> findAll() { return sessionRepository.findAll(); }
 
+    /** [A2] Danh sách kiểm kê đã lọc theo phạm vi kho của user. */
+    public List<StocktakeSession> findAllScoped() { return sessionRepository.findAllScoped(); }
+
     public List<StocktakeDetail> findDetails(String sessionId) {
         return detailRepository.findBySessionId(sessionId);
     }
diff --git a/backend/src/main/java/org/lvtn/mws/domain/service/TransferOrderDomainService.java b/backend/src/main/java/org/lvtn/mws/domain/service/TransferOrderDomainService.java
index 3403620..f0eccf1 100644
--- a/backend/src/main/java/org/lvtn/mws/domain/service/TransferOrderDomainService.java
+++ b/backend/src/main/java/org/lvtn/mws/domain/service/TransferOrderDomainService.java
@@ -82,6 +82,11 @@ public class TransferOrderDomainService {
         return transferRepository.findAll();
     }
 
+    /** [A2] Danh sách phiếu điều chuyển đã lọc theo phạm vi kho của user. */
+    public List<TransferOrder> findAllScoped() {
+        return transferRepository.findAllScoped();
+    }
+
     // ──────────────────────────────────────────────────────────────────────────
     // I. KHỞI TẠO PHIẾU (DRAFT)
     // ──────────────────────────────────────────────────────────────────────────
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/AdjustmentVoucherRepositoryImpl.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/AdjustmentVoucherRepositoryImpl.java
index 8f2ed7b..208bc26 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/AdjustmentVoucherRepositoryImpl.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/AdjustmentVoucherRepositoryImpl.java
@@ -31,6 +31,13 @@ public class AdjustmentVoucherRepositoryImpl implements IAdjustmentVoucherReposi
         return jpa.findAll().stream().map(mapper::toDomain).toList();
     }
 
+    @Override
+    public List<AdjustmentVoucher> findAllScoped() {
+        return jpa.findAll(org.lvtn.mws.infrastructure.security.scope.WarehouseScopeSpecs
+                        .<org.lvtn.mws.infrastructure.persistence.entity.AdjustmentVoucherEntity>restrict("warehouseId"))
+                .stream().map(mapper::toDomain).toList();
+    }
+
     @Override
     public List<AdjustmentVoucher> findBySessionId(String sessionId) {
         return jpa.findBySessionId(sessionId).stream().map(mapper::toDomain).toList();
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/JpaAdjustmentVoucherRepository.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/JpaAdjustmentVoucherRepository.java
index 0057ad9..8617375 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/JpaAdjustmentVoucherRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/adjustment/JpaAdjustmentVoucherRepository.java
@@ -2,10 +2,13 @@ package org.lvtn.mws.infrastructure.persistence.repository.adjustment;
 
 import org.lvtn.mws.infrastructure.persistence.entity.AdjustmentVoucherEntity;
 import org.springframework.data.jpa.repository.JpaRepository;
+import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
 
 import java.util.List;
 
-public interface JpaAdjustmentVoucherRepository extends JpaRepository<AdjustmentVoucherEntity, String> {
+public interface JpaAdjustmentVoucherRepository
+        extends JpaRepository<AdjustmentVoucherEntity, String>,
+                JpaSpecificationExecutor<AdjustmentVoucherEntity> {
     List<AdjustmentVoucherEntity> findBySessionId(String sessionId);
     boolean existsByVoucherNumber(String voucherNumber);
     long countByVoucherNumberStartingWith(String prefix);
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/JpaSalesOrderRepository.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/JpaSalesOrderRepository.java
index e35769d..1c2df83 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/JpaSalesOrderRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/JpaSalesOrderRepository.java
@@ -2,10 +2,13 @@ package org.lvtn.mws.infrastructure.persistence.repository.salesorder;
 
 import org.lvtn.mws.infrastructure.persistence.entity.SalesOrderEntity;
 import org.springframework.data.jpa.repository.JpaRepository;
+import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
 
 import java.util.List;
 
-public interface JpaSalesOrderRepository extends JpaRepository<SalesOrderEntity, String> {
+public interface JpaSalesOrderRepository
+        extends JpaRepository<SalesOrderEntity, String>,
+                JpaSpecificationExecutor<SalesOrderEntity> {
     List<SalesOrderEntity> findByStatus(String status);
     boolean existsBySoNumber(String soNumber);
     long countBySoNumberStartingWith(String prefix);
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/SalesOrderRepositoryImpl.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/SalesOrderRepositoryImpl.java
index 5d5d6ef..01a848b 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/SalesOrderRepositoryImpl.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/salesorder/SalesOrderRepositoryImpl.java
@@ -32,6 +32,13 @@ public class SalesOrderRepositoryImpl implements ISalesOrderRepository {
         return jpa.findAll().stream().map(mapper::toDomain).toList();
     }
 
+    @Override
+    public List<SalesOrder> findAllScoped() {
+        return jpa.findAll(org.lvtn.mws.infrastructure.security.scope.WarehouseScopeSpecs
+                        .<org.lvtn.mws.infrastructure.persistence.entity.SalesOrderEntity>restrict("warehouseId"))
+                .stream().map(mapper::toDomain).toList();
+    }
+
     @Override
     public List<SalesOrder> findByStatus(Status status) {
         return jpa.findByStatus(status.name()).stream().map(mapper::toDomain).toList();
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/JpaStocktakeSessionRepository.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/JpaStocktakeSessionRepository.java
index f4cc500..2c8e276 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/JpaStocktakeSessionRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/JpaStocktakeSessionRepository.java
@@ -2,11 +2,14 @@ package org.lvtn.mws.infrastructure.persistence.repository.stocktake;
 
 import org.lvtn.mws.infrastructure.persistence.entity.StocktakeSessionEntity;
 import org.springframework.data.jpa.repository.JpaRepository;
+import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
 
 import java.util.List;
 import java.util.Optional;
 
-public interface JpaStocktakeSessionRepository extends JpaRepository<StocktakeSessionEntity, String> {
+public interface JpaStocktakeSessionRepository
+        extends JpaRepository<StocktakeSessionEntity, String>,
+                JpaSpecificationExecutor<StocktakeSessionEntity> {
     List<StocktakeSessionEntity> findByWarehouseId(String warehouseId);
     Optional<StocktakeSessionEntity> findFirstByWarehouseIdAndStatus(String warehouseId, String status);
     boolean existsByWarehouseIdAndStatus(String warehouseId, String status);
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/StocktakeSessionRepositoryImpl.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/StocktakeSessionRepositoryImpl.java
index 3a61d0a..334cfe6 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/StocktakeSessionRepositoryImpl.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/stocktake/StocktakeSessionRepositoryImpl.java
@@ -33,6 +33,13 @@ public class StocktakeSessionRepositoryImpl implements IStocktakeSessionReposito
         return jpa.findAll().stream().map(mapper::toDomain).toList();
     }
 
+    @Override
+    public List<StocktakeSession> findAllScoped() {
+        return jpa.findAll(org.lvtn.mws.infrastructure.security.scope.WarehouseScopeSpecs
+                        .<org.lvtn.mws.infrastructure.persistence.entity.StocktakeSessionEntity>restrict("warehouseId"))
+                .stream().map(mapper::toDomain).toList();
+    }
+
     @Override
     public List<StocktakeSession> findByWarehouseId(String warehouseId) {
         return jpa.findByWarehouseId(warehouseId).stream().map(mapper::toDomain).toList();
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/JpaTransferOrderRepository.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/JpaTransferOrderRepository.java
index 1f95056..ec4c402 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/JpaTransferOrderRepository.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/JpaTransferOrderRepository.java
@@ -3,10 +3,13 @@ package org.lvtn.mws.infrastructure.persistence.repository.transfer;
 import org.lvtn.mws.domain.model.TransferOrder;
 import org.lvtn.mws.infrastructure.persistence.entity.TransferOrderEntity;
 import org.springframework.data.jpa.repository.JpaRepository;
+import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
 
 import java.util.List;
 
-public interface JpaTransferOrderRepository extends JpaRepository<TransferOrderEntity, String> {
+public interface JpaTransferOrderRepository
+        extends JpaRepository<TransferOrderEntity, String>,
+                JpaSpecificationExecutor<TransferOrderEntity> {
     List<TransferOrderEntity> findByStatus(TransferOrder.Status status);
     boolean existsByTransferNumber(String transferNumber);
 }
diff --git a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/TransferOrderRepositoryImpl.java b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/TransferOrderRepositoryImpl.java
index f1e8815..8677d62 100644
--- a/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/TransferOrderRepositoryImpl.java
+++ b/backend/src/main/java/org/lvtn/mws/infrastructure/persistence/repository/transfer/TransferOrderRepositoryImpl.java
@@ -63,6 +63,18 @@ public class TransferOrderRepositoryImpl implements ITransferOrderRepository {
         return result;
     }
 
+    @Override
+    public List<TransferOrder> findAllScoped() {
+        org.springframework.data.jpa.domain.Specification<TransferOrderEntity> spec =
+                org.lvtn.mws.infrastructure.security.scope.WarehouseScopeSpecs
+                        .restrictAny("fromWarehouseId", "toWarehouseId");
+        List<TransferOrder> result = new ArrayList<>();
+        for (TransferOrderEntity e : jpaTransferOrderRepository.findAll(spec)) {
+            result.add(assemble(e));
+        }
+        return result;
+    }
+
     @Override
     public List<TransferOrder> findByStatus(TransferOrder.Status status) {
         List<TransferOrder> result = new ArrayList<>();
