diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 0cb4259..6e581b1 100644
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -1,3 +1,55 @@
-// App gốc: routing đã đặt ở routes/index.jsx (được gọi trong main.jsx).
-// Giữ file này để tương thích cấu trúc quen thuộc.
-export { default } from './routes/index.jsx'
+import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
+import { BrowserRouter } from 'react-router-dom'
+import { ConfigProvider, App as AntdApp, theme } from 'antd'
+import viVN from 'antd/locale/vi_VN'
+import { AuthProvider } from './auth/AuthContext.jsx'
+import { NotificationProvider } from './context/NotificationContext.jsx'
+import { useThemeMode } from './context/ThemeContext.jsx'
+import ErrorBoundary from './components/ErrorBoundary.jsx'
+import AppRoutes from './routes/index.jsx'
+
+// Tồn kho có optimistic-lock (409) => cho retry nhẹ; lỗi 4xx khác thì không retry.
+const queryClient = new QueryClient({
+  defaultOptions: {
+    queries: {
+      retry: (failureCount, err) => {
+        const s = err?.response?.status
+        if (s && s >= 400 && s < 500 && s !== 409) return false
+        return failureCount < 2
+      },
+      staleTime: 30_000,
+      refetchOnWindowFocus: false,
+    },
+  },
+})
+
+// Đứng TRONG <ThemeProvider> (xem main.jsx) để ConfigProvider đọc được chế độ
+// sáng/tối. Tách khỏi main.jsx để main.jsx chỉ còn mỗi việc mount — react-refresh
+// yêu cầu file chứa component phải export component đó.
+export default function App() {
+  const { isDark } = useThemeMode()
+  return (
+    <ConfigProvider
+      locale={viVN}
+      theme={{
+        algorithm: isDark ? theme.darkAlgorithm : theme.defaultAlgorithm,
+        token: { colorPrimary: '#1677ff' },
+      }}>
+      <AntdApp>
+        {/* Chốt chặn cuối: lỗi ở tầng provider/route cũng không để màn hình trắng.
+            (AppLayout còn 1 boundary nữa bọc riêng từng trang.) */}
+        <ErrorBoundary>
+          <QueryClientProvider client={queryClient}>
+            <BrowserRouter>
+              <AuthProvider>
+                <NotificationProvider>
+                  <AppRoutes />
+                </NotificationProvider>
+              </AuthProvider>
+            </BrowserRouter>
+          </QueryClientProvider>
+        </ErrorBoundary>
+      </AntdApp>
+    </ConfigProvider>
+  )
+}
diff --git a/frontend/src/__smoke__/smoke.test.jsx b/frontend/src/__smoke__/smoke.test.jsx
index c9804d6..218d409 100644
--- a/frontend/src/__smoke__/smoke.test.jsx
+++ b/frontend/src/__smoke__/smoke.test.jsx
@@ -13,7 +13,6 @@ import { render, screen, waitFor, fireEvent } from '@testing-library/react'
 import { MemoryRouter } from 'react-router-dom'
 import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
 import { ConfigProvider, App as AntdApp } from 'antd'
-import React from 'react'
 
 // Chặn mọi lời gọi API: test này không cần BE.
 // Hình dạng trả về phải KHỚP BE thật, nếu không thì test báo lỗi giả:
@@ -57,7 +56,7 @@ vi.mock('../ws/notificationClient', () => ({
 
 // jsdom chưa có 2 API này; antd + FitTable cần.
 beforeEach(() => {
-  global.ResizeObserver = class { observe() {} unobserve() {} disconnect() {} }
+  globalThis.ResizeObserver = class { observe() {} unobserve() {} disconnect() {} }
   window.matchMedia = window.matchMedia || ((q) => ({
     matches: false, media: q, onchange: null,
     addEventListener: () => {}, removeEventListener: () => {},
@@ -153,7 +152,7 @@ describe('Bấm nút "Tạo ..." không nổ', () => {
             <Route path={`${path}/:id`} element={<Mod />} />
           </Routes>
         ))
-        const btn = await screen.findByRole('button', { name: new RegExp(label) })
+        const btn = await screen.findByRole('button', { name: new RegExp(label) }, { timeout: 8000 })
         fireEvent.click(btn)
         await waitFor(() => expect(document.body.textContent.length).toBeGreaterThan(0))
         await new Promise(r => setTimeout(r, 30)) // để lỗi bất đồng bộ kịp nổi lên
diff --git a/frontend/src/auth/AuthContext.jsx b/frontend/src/auth/AuthContext.jsx
index d36f8ca..a3210d4 100644
--- a/frontend/src/auth/AuthContext.jsx
+++ b/frontend/src/auth/AuthContext.jsx
@@ -92,4 +92,5 @@ export function useAuth() {
   return ctx
 }
 
+// eslint-disable-next-line react-refresh/only-export-components -- re-export tiện dụng
 export { getErrorMessage }
diff --git a/frontend/src/components/BarcodeScanner.jsx b/frontend/src/components/BarcodeScanner.jsx
index 9da5b8f..5ba013b 100644
--- a/frontend/src/components/BarcodeScanner.jsx
+++ b/frontend/src/components/BarcodeScanner.jsx
@@ -23,6 +23,8 @@ export default function BarcodeScanner({ onScan, paused, onActiveChange }) {
   useEffect(() => { pausedRef.current = paused }, [paused])
   useEffect(() => { onActiveChange?.(on) }, [on, onActiveChange])
 
+  function stop() { try { controlsRef.current?.stop() } catch { /* noop */ } }
+
   useEffect(() => {
     if (!on) { stop(); return }
     let cancelled = false
@@ -61,7 +63,6 @@ export default function BarcodeScanner({ onScan, paused, onActiveChange }) {
     // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [on, deviceId])
 
-  function stop() { try { controlsRef.current?.stop() } catch { /* noop */ } }
 
   if (!secure) {
     return (
diff --git a/frontend/src/context/NotificationContext.jsx b/frontend/src/context/NotificationContext.jsx
index 94e73ec..bb78754 100644
--- a/frontend/src/context/NotificationContext.jsx
+++ b/frontend/src/context/NotificationContext.jsx
@@ -26,7 +26,10 @@ export function NotificationProvider({ children }) {
 
   // Kết nối WebSocket khi đã đăng nhập; nhận tin -> làm mới inbox + đếm.
   useEffect(() => {
-    if (!canRead) { setConnected(false); return }
+    // Không setConnected(false) ở đây: state chỉ phản ánh TRẠNG THÁI WS.
+    // Việc "chưa đăng nhập thì coi như mất kết nối" được suy ra lúc đọc
+    // (xem `connected:` bên dưới) thay vì set trong thân effect.
+    if (!canRead) return
     const token = tokenStore.access
     if (!token) return
     const client = createNotificationClient({
@@ -40,7 +43,7 @@ export function NotificationProvider({ children }) {
         qc.invalidateQueries({ queryKey: ['notif-unread'] })
       },
     })
-    return () => { client.deactivate() }
+    return () => { client.deactivate(); setConnected(false) }
   }, [canRead, user?.userId]) // eslint-disable-line react-hooks/exhaustive-deps
 
   const invalidate = () => {
@@ -60,7 +63,8 @@ export function NotificationProvider({ children }) {
     notifications: inbox.data || [],
     loading: inbox.isLoading,
     unreadCount: readUnread(unread.data),
-    connected,
+    // WS nối được NHƯNG không còn quyền đọc => vẫn coi là chưa kết nối.
+    connected: canRead && connected,
     canRead,
     refetch: () => { inbox.refetch(); unread.refetch() },
     markRead: (id) => markReadMut.mutate(id),
diff --git a/frontend/src/context/ThemeContext.jsx b/frontend/src/context/ThemeContext.jsx
index db87315..fb6c046 100644
--- a/frontend/src/context/ThemeContext.jsx
+++ b/frontend/src/context/ThemeContext.jsx
@@ -47,6 +47,7 @@ export function ThemeProvider({ children }) {
   return <ThemeCtx.Provider value={value}>{children}</ThemeCtx.Provider>
 }
 
+// eslint-disable-next-line react-refresh/only-export-components -- context + hook cùng file là mẫu chuẩn; chỉ ảnh hưởng hot-reload lúc dev
 export function useThemeMode() {
   const v = useContext(ThemeCtx)
   if (!v) throw new Error('useThemeMode phải nằm trong <ThemeProvider>')
diff --git a/frontend/src/hooks/useBinLabels.js b/frontend/src/hooks/useBinLabels.js
index c6f6205..d3f8ca8 100644
--- a/frontend/src/hooks/useBinLabels.js
+++ b/frontend/src/hooks/useBinLabels.js
@@ -27,7 +27,6 @@ export function useBinLabels() {
     const m = {}
     results.forEach(r => (r.data || []).forEach(b => { m[b.id] = b.coordinateLabel || b.id }))
     return m
-    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [results])
 
   // Trả nhãn ô kệ; nếu chưa nạp được thì fallback về id (không vỡ giao diện).
diff --git a/frontend/src/main.jsx b/frontend/src/main.jsx
index 0bcda98..9b733ee 100644
--- a/frontend/src/main.jsx
+++ b/frontend/src/main.jsx
@@ -1,68 +1,17 @@
 import React from 'react'
 import { createRoot } from 'react-dom/client'
-import { BrowserRouter } from 'react-router-dom'
-import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
-import { ConfigProvider, App as AntdApp, theme } from 'antd'
-import viVN from 'antd/locale/vi_VN'
 import dayjs from 'dayjs'
 import 'dayjs/locale/vi'
-import { AuthProvider } from './auth/AuthContext.jsx'
-import { NotificationProvider } from './context/NotificationContext.jsx'
-import { ThemeProvider, useThemeMode } from './context/ThemeContext.jsx'
-import ErrorBoundary from './components/ErrorBoundary.jsx'
-import AppRoutes from './routes/index.jsx'
+import { ThemeProvider } from './context/ThemeContext.jsx'
+import App from './App.jsx'
 import './index.css'
 
 dayjs.locale('vi')
 
-// Tồn kho có optimistic-lock (409) => cho retry nhẹ; lỗi 4xx khác thì không retry.
-const queryClient = new QueryClient({
-  defaultOptions: {
-    queries: {
-      retry: (failureCount, err) => {
-        const s = err?.response?.status
-        if (s && s >= 400 && s < 500 && s !== 409) return false
-        return failureCount < 2
-      },
-      staleTime: 30_000,
-      refetchOnWindowFocus: false,
-    },
-  },
-})
-
-// ConfigProvider phải nằm TRONG ThemeProvider để đọc được chế độ sáng/tối.
-function ThemedApp() {
-  const { isDark } = useThemeMode()
-  return (
-    <ConfigProvider
-      locale={viVN}
-      theme={{
-        algorithm: isDark ? theme.darkAlgorithm : theme.defaultAlgorithm,
-        token: { colorPrimary: '#1677ff' },
-      }}>
-      <AntdApp>
-        {/* Chốt chặn cuối: lỗi ở tầng provider/route cũng không để màn hình trắng.
-            (AppLayout còn 1 boundary nữa bọc riêng từng trang.) */}
-        <ErrorBoundary>
-          <QueryClientProvider client={queryClient}>
-            <BrowserRouter>
-              <AuthProvider>
-                <NotificationProvider>
-                  <AppRoutes />
-                </NotificationProvider>
-              </AuthProvider>
-            </BrowserRouter>
-          </QueryClientProvider>
-        </ErrorBoundary>
-      </AntdApp>
-    </ConfigProvider>
-  )
-}
-
 createRoot(document.getElementById('root')).render(
   <React.StrictMode>
     <ThemeProvider>
-      <ThemedApp />
+      <App />
     </ThemeProvider>
   </React.StrictMode>
 )
diff --git a/frontend/vite.config.js b/frontend/vite.config.js
index ec020af..fe52d96 100644
--- a/frontend/vite.config.js
+++ b/frontend/vite.config.js
@@ -12,23 +12,31 @@ export default defineConfig(({ mode }) => {
     // sockjs-client tham chiếu biến 'global' của Node -> map sang globalThis.
     define: { global: 'globalThis' },
     build: {
-      // Mỗi trang đã là 1 chunk riêng (React.lazy trong routes/index.jsx).
-      // Phần còn lại là thư viện: tách theo vòng đời cập nhật để trình duyệt
-      // cache được lâu — sửa code trang không làm hết hạn cache của antd.
-      rollupOptions: {
-        output: {
-          manualChunks(id) {
-            if (!id.includes('node_modules')) return
-            if (id.includes('/xlsx')) return 'vendor-xlsx'        // chỉ tải khi xuất/nhập Excel
-            if (id.includes('/@zxing')) return 'vendor-zxing'      // chỉ tải khi bật camera
-            if (id.includes('/sockjs') || id.includes('/@stomp')) return 'vendor-ws'
-            if (id.includes('/antd/') || id.includes('/@ant-design/') || id.includes('/rc-')) return 'vendor-antd'
-            if (id.includes('/react-dom/') || id.includes('/react/') || id.includes('/react-router')) return 'vendor-react'
-            return 'vendor'
-          },
-        },
-      },
-      chunkSizeWarningLimit: 700,
+      // KHÔNG dùng manualChunks — cố ý.
+      //
+      // Đã thử gom vendor thủ công (vendor-react / vendor-antd / vendor) và kết
+      // quả TỆ HƠN mặc định ở cả hai mặt:
+      //   1. Sinh vòng lặp chunk: react-dom kéo `scheduler` (rơi vào 'vendor')
+      //      => vendor-react -> vendor; còn react-query trong 'vendor' lại kéo
+      //      react => vendor -> vendor-react. Rollup cảnh báo "Circular chunk".
+      //      Vá bằng cách liệt kê thêm scheduler/cookie/... là trò đuổi bắt.
+      //   2. Nặng hơn: ép TOÀN BỘ antd (1,1 MB) vào chunk khởi động. Rollup mặc
+      //      định tự tách antd theo component — vd Table thành chunk riêng
+      //      165 KB, chỉ tải khi vào trang có bảng.
+      //
+      // Số đo thật (cùng source):
+      //   gốc, 1 bundle        2.660 KB thô / 812 KB gzip
+      //   manualChunks thủ công 1.531 KB / 487 KB  + 2 cảnh báo circular
+      //   mặc định (bản này)    1.028 KB / 330 KB  + không cảnh báo
+      //
+      // Việc tách xlsx/@zxing/sockjs KHÔNG cần manualChunks: chúng chỉ được với
+      // tới qua `await import()` (utils/excel.js, BarcodeScanner,
+      // ws/notificationClient.js) nên Rollup vốn đã đẩy ra chunk riêng.
+      //
+      // Chunk khởi động ~1 MB gần như toàn bộ là antd core + locale + dayjs,
+      // dùng ở mọi trang nên không tách thêm được. Nâng ngưỡng cảnh báo cho
+      // đúng thực tế thay vì để build kêu mỗi lần.
+      chunkSizeWarningLimit: 1100,
     },
     server: {
       port: 5173,
diff --git a/frontend/vitest.config.js b/frontend/vitest.config.js
index 90f2805..b9a11c6 100644
--- a/frontend/vitest.config.js
+++ b/frontend/vitest.config.js
@@ -4,5 +4,13 @@ import react from '@vitejs/plugin-react'
 export default defineConfig({
   plugins: [react()],
   define: { global: 'globalThis' },
-  test: { environment: 'jsdom', globals: false, include: ['src/**/*.test.jsx'] },
+  test: {
+    environment: 'jsdom',
+    globals: false,
+    include: ['src/**/*.test.jsx'],
+    // Lần đầu import một trang lớn (TransferOrdersPage + antd) có thể mất >1s;
+    // timeout mặc định 5s của vitest và 1s của findBy* từng làm suite đỏ giả một
+    // lần trên máy chậm. Nới ra cho khỏi nhấp nháy — test này không đo tốc độ.
+    testTimeout: 20000,
+  },
 })
